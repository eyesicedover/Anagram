<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
        <ul>
            <p>WOAH</p>
            @foreach (var list in Model)
            {
                @foreach (var item in @list)
                {
                    <li>@item</li>
                }
            }
        </ul>
    </body>
</html>
